diff --git a/PKGBUILD b/PKGBUILD
index ea76ea4..325a212 100644
--- a/PKGBUILD
+++ b/PKGBUILD
@@ -35,6 +35,7 @@ build() {
     _kernver="$(cat /usr/lib/modules/${_extramodules}/version)"
 
     cd "${_pkg}"
+    LD=ld.lld \
     make -C kernel SYSSRC=/usr/lib/modules/"${_kernver}/build" module
 }
 
