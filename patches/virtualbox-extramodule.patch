diff --git a/PKGBUILD b/PKGBUILD
index 67f661b..e820e4a 100644
--- a/PKGBUILD
+++ b/PKGBUILD
@@ -28,7 +28,7 @@ build() {
 
   # build host modules
   echo 'Host modules'
-  fakeroot dkms build --dkmstree "$srcdir" -m vboxhost/${pkgver}_OSE -k ${_kernver}
+  sudo -u phoepsilonix dkms build --dkmstree "$srcdir" -m vboxhost/${pkgver}_OSE -k ${_kernver}
 }
 
 package(){
